# ── Environnement ──
.env
.env.*
!.env.example

# ── IDE ──
.idea
.vscode

# ── OS ──
.DS_Store
Thumbs.db

# ── Node ──
node_modules
frontend/dist

# ── Python ──
__pycache__
*.pyc
*.pyo
.venv
venv

# ── Docker volumes / data ──
services/orchestrator/uploads/
services/orchestrator/results/

# ── eval & logs
# Seuls les sorties et les documents générés sont ignorés : une règle `eval`
# entière rendait inertes les scripts et dossiers de test versionnés, et faisait
# échouer tout `git add` sur eux.
eval/results/
eval/test_docs/
services/logs/*.md

# ── tests
.pytest_cache
.coverage

# ── Formulaires medForms récupérés par tools/gen_catalog.py ──
# Les PDF vierges appartiennent à medForms et pèsent ~2 Mo pièce : on versionne
# la liste (tools/catalog_fr.txt) et les templates dérivés, pas les binaires.
# Les récupérer avec :
#   cd services/orchestrator && python -m tools.gen_catalog --list tools/catalog_fr.txt --write
services/orchestrator/forms/*.pdf
services/orchestrator/.medforms_cache/